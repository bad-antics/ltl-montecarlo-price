// ltl-montecarlo-price/main.ltl
//
// Monte-Carlo path-dependent option pricer.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-montecarlo-price v0.1.0")
    io::println("Monte-Carlo path-dependent option pricer.")
    0
}
